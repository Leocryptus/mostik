import { redirect } from "next/navigation";

/**
 * Инбокс переехал внутрь вкладки «День»: разбор мелочёвки — часть сбора дня,
 * а не отдельный экран (ТЗ §19.5). Страницу оставляем редиректом, чтобы старые
 * ссылки и закладки не давали 404.
 */
export default function InboxPage() {
  redirect("/day");
}
